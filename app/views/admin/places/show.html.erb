<div class="container-fluid">
    <div class="container">
       <!-- <div class="row"> 
        <div class="col-md-3 col-lg-6 col-sm-3 ">
          <div id="chartContainer" style="height: 200px; width: 100%;"></div>
        </div>  
        <div class="col-md-3 col-lg-6 col-sm-3 ">
          <div id="chartContainer2" style="height: 200px; width: 100%;"></div> 
        </div> 
       </div>  -->
        <!-- {% for flashMessage in app.session.flashbag.get('mensaje') %}
        <div class="row">
          <div class="col-xs-12">
          	<div class="alert alert-success" role="alert">
          		<div class="container"> {{ flashMessage }} </div>
          	</div>
          </div>
        </div>
        {% endfor %} -->
        
        <div class="row">
          <div class="col-xs-12">
            <h1 class="page-header"><%= t('titulos.perfilsidre') %></h1>
            </div>
        </div>
        
        <div class="row">
          <div class="col-xs-6">
              <h1 class="card-title"><%= @place.name %></h1>
              <p class="card-description" id="descripcion"><%= @place.description %></p>
              <p class="card-description" id="horario"><%= @place.time %></p>
              <%= link_to t('boton.editar'), edit_admin_place_path(@place), class: 'btn btn-primary' %>
         </div>
         <div class="col-xs-6">
           <div class="card-single-direction text-uppercase text-center"><h4><%= @place.town %></h4></div>
           <img src= "<%= @place.picture %>"  class="img-responsive single-img img-rounded" alt="Responsive image">
          </div>
        </div>
    </div>

    <div class="row">
        <div class="col-xs-9 col-xs-offset-1">
          <h2 class="page-header"><%= t('titulos.listakupelas') %></h2>
        </div>
        <div class="col-xs-1">
            <div class="add-kupela pull-right">
            <%= link_to new_admin_place_item_path(@place.id) do %>
                <i id= "plus" class="fa fa-plus-square-o fa-4x" aria-hidden="true"></i></a>
            <% end %>
            </div>
        </div>  
        <div class="col-xs-10 col-xs-offset-1">
            
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th><%= t('labels.kupela.foto') %></th>
                    <th><%= t('labels.kupela.nombre') %></th>
                    <th><%= t('labels.kupela.descripcion') %></th>
                    <th><%= t('labels.kupela.ano') %></th>
                    <th colspan="2">Actions:</th>
                  </tr>
                </thead>
                <tbody>
                  <%= render @place.items %>
                </tbody>
              </table>             
            </div>
        </div>
    </div>

      
      

      <!-- <script src={{asset("js/estadisticas.min.js")}}></script>
      <script type="text/javascript">
      
      window.onload = function () {
      	var chart = new CanvasJS.Chart("chartContainer", {
      		theme: "theme2",//theme1
      		title:{
      			text: "Likes a las kupelas por GENERO"              
      		},
      		animationEnabled: false,   // change to true
      		data: [              
      		{
      			// Change type to "bar", "area", "spline", "pie",etc.
      			type: "pie",
      			dataPoints: [
      				{ label: "Mujeres",  y: {{mujeres[0].mujeres}}},
      				{ label: "Hombres", y: {{hombres[0].hombres}}}
      				
      			]
      		}
      		]
      	});
      	chart.render();
        	
        	
        	
      	var chart2 = new CanvasJS.Chart("chartContainer2", {
      		theme: "theme2",//theme1
      		title:{
      			text: "Numero Votos por fecha"              
      		},
      		animationEnabled: false,   // change to true
      		data: [              
      		{
      			// Change type to "bar", "area", "spline", "pie",etc.
      			type: "column",
      			dataPoints: [
      			  {% for fecha in fechas %}
      			    	{ label: "{{fecha.fecha | date("d/m/Y")}}",  y: {{fecha.NumVotos}} },
      			  {% endfor %}
      	
      			]
      		}
      		]
      	});
      	chart2.render();
      }
      </script> -->
