<div class="row">
    <div class="col-xs-12">
        <h1 class="mtitle text-center"><%= t('paginas.sidrerias.titulo') %></h1>
    </div>
    <% @places.each do |place| %>
    
    <div class="col-xs-6 col-lg-4">
        <div class="card">
        	<%= link_to place_path(place), { :class => 'card-link', :name => place.name } do %>
	            <div class="card-direction text-uppercase text-center"><h4><%= place.town %></h4></div>
	            <img class="card-image img-responsive" src="<%= place.picture %>"></img>
	            <h2 class="card-title text-uppercase"><%= place.name %></h2>
	            <p class="card-description"><%= place.description %></p>
            <% end %>
        </div>
    </div>
    <% end %>
</div>